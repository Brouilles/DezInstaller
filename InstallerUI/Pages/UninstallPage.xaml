<Page x:Class="InstallerUI.Pages.UninstallPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InstallerUI.Pages"
             xmlns:p="clr-namespace:InstallerUI.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="588"
             Title="UninstallPage" Height="371" Loaded="Page_Loaded">
    <StackPanel Margin="10">
        <TextBlock Margin="0,10,0,0" FontSize="24" FontWeight="Bold" Foreground="White">
            <Run Text="Uninstall"/>
            <Run Text="{x:Static p:Resource.APP_NAME}"/>
        </TextBlock>
        <TextBlock Margin="0,10,0,0" Foreground="White">
            <Run Text="Remove"/>
            <Run Text="{x:Static p:Resource.APP_NAME}"/>
            <Run Text="from your computer."/>
        </TextBlock>
        <TextBlock Margin="0,10,0,0" Foreground="White">
            <Run Text="{x:Static p:Resource.APP_NAME}"/>
            <Run Text="will be uninstalled. Click Uninstall to start the uninstallation."/>
        </TextBlock>
    </StackPanel>
</Page>
